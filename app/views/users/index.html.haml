= stylesheet_link_tag 'users'
Antall registrerte brukere:
= @users.length
%br
Antall admins:
= @users.where(admin: true).length
%table{class: "table table-striped table-hover table-condensed"}
  %thead
    %th Brukernavn
    %th Navn
    %th Mobilnummer
    %th Email
    %th Admin?
    %th Registrert
    %th Sist endret
    %th Sist sett
    %th
  - @users.each do |u|
    %tr
      %td
        = link_to u.username, edit_user_path(u)
      %td
        = u.name
      %td
        = u.mobile
      %td
        = u.email
      %td
        = t u.admin.to_s
      %td
        = u.created_at.to_formatted_s(:short)
      %td
        = u.updated_at.to_formatted_s(:short)
      %td
        = u.last_login.to_formatted_s(:short)
      %td
        - if current_user.id == 1
          = link_to 'x', u, method: :delete, data: {confirm: "Er du sikker?"}
