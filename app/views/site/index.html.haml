= content_for :title, 'Forside'
= cache(front_page_cache_key, expires_in: 5.minutes) do
  .tile
    .tile.is-ancestor
      %h2.page-header
        = @pages.first.title
      = raw(kramdown(@pages.first.content))
    - @pages[1..-1].each do |p|
      .col-md-6
        %h2.page-header
          = p.title
        = raw(kramdown(p.content))
